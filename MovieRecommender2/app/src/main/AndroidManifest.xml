<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movierecommender">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ParasiteActivity"
            android:label="parasiteActivity" >
        </activity>
        <activity
            android:name=".AladdinActivity"
            android:label="AladdinActivity" >
        </activity>
        <activity
            android:name=".AvatarActivity"
            android:label="AvatarActivity" >
        </activity>
        <activity
            android:name=".TitanicActivity"
            android:label="TitaniActivity" >
        </activity>
        <activity
            android:name=".ClockActivity"
            android:label="ClockActivity" >
        </activity>
    </application>
</manifest>